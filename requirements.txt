# Additional Python dependencies beyond the base image.
#
# Base image already provides (DO NOT re-pin):
#   pyspark==3.5.0
#   delta-spark==3.1.0
#   pandas==2.1.0
#   pyarrow==14.0.0
#   pyyaml==6.0.1
#   duckdb==0.10.0
#
# Add only packages genuinely absent from the base image.
# All packages must be installable from PyPI (no private indices).
